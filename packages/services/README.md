# RadioLink Services

Shared higher-level services used by multiple RadioLink modules and host applications.

Planned responsibilities include device/provider registry, module registry, persistence abstractions, diagnostics/logging, mesh node/channel state, situational event state and optional APRS/IP/TAK bridges.
