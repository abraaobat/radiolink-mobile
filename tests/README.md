# Tests

Cross-platform fixtures, captured frames and interoperability tests. Initial priority: KISS framing, AX.25 parsing and APRS encode/decode using deterministic packet fixtures before real RF validation.
